{
  "name": "free-ai-gateway",
  "version": "2.0.0",
  "description": "Free AI Gateway - Multi-provider AI routing with failover",
  "scripts": {
    "dev": "uvicorn app.main:app --reload",
    "dev:voice": "DISABLE_DATABASE=1 GATEWAY_SECRET= REQUEST_TIMEOUT_S=8 MAX_RETRIES_PER_PROVIDER=0 ROUTING_MODE=chain uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "build": "cd ui && npm run build",
    "build:ui": "npm run build --workspace ui",
    "start": "uvicorn app.main:app --host 0.0.0.0 --port 8000"
  },
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "packageManager": "npm@10.0.0",
  "workspaces": [
    "ui"
  ]
}
